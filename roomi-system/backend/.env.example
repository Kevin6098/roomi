# File: backend/.env.example
# Copy to .env and fill in your values

PORT=3000
NODE_ENV=development

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=roomi_db

# CORS (optional: frontend origin for production)
# CORS_ORIGIN=http://localhost:5500
